apiVersion: v1
kind: Service
metadata:
  name: {{ include "upgradeResponder.fullname" . }}
  labels:
    {{- include "upgradeResponder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "upgradeResponder.selectorLabels" . | nindent 4 }}
