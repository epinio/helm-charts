apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "upgradeResponder.fullname" . }}
  labels:
    {{- include "upgradeResponder.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "upgradeResponder.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/upgradeResponderConfigMap: {{ include (print $.Template.BasePath "/upgradeResponderConfigMap.yaml") . | sha256sum }}
      labels:
        {{- include "upgradeResponder.labels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
          - name: APPLICATION_NAME
            value: {{ .Values.applicationName }}
          - name: INFLUXDB_URL
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secret.name }}
                key: influxDBUrl
          - name: INFLUXDB_USER
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secret.name }}
                key: influxDBUser
          - name: INFLUXDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secret.name }}
                key: influxDBPassword
          - name: PORT
            value: "{{ .Values.service.port }}"
          - name: CACHE_SYNC_INTERVAL
            value: "{{ .Values.flags.cacheSyncInterval }}"
          - name: CACHE_SIZE
            value: "{{ .Values.flags.cacheSize }}"
          command:
          - upgrade-responder
          args:
          - --debug
          - start
          - --application-name
          - $(APPLICATION_NAME)
          - --upgrade-response-config
          - /run/secrets/response-config.json
          - --request-schema
          - /run/secrets/request-schema.json
          - --influxdb-url
          - $(INFLUXDB_URL)
          - --influxdb-user
          - $(INFLUXDB_USER)
          - --influxdb-pass
          - $(INFLUXDB_PASSWORD)
          - --port
          - $(PORT)
          - --cache-sync-interval
          - $(CACHE_SYNC_INTERVAL)
          - --cache-size
          - $(CACHE_SIZE)
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          # livenessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          volumeMounts:
          - mountPath: /run/secrets/response-config.json
            name: {{ include "upgradeResponder.upgradeResponderConfigMapName" . }}
            subPath: response-config.json
          - mountPath: /run/secrets/request-schema.json
            name: {{ include "upgradeResponder.upgradeResponderConfigMapName" . }}
            subPath: request-schema.json
          resources:
{{ toYaml .Values.resources | indent 12 }}
    {{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
      volumes:
      - name: {{ include "upgradeResponder.upgradeResponderConfigMapName" . }}
        configMap:
          name: {{ include "upgradeResponder.upgradeResponderConfigMapName" . }}
